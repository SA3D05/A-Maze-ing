[project]
name = "mazegen"
version = "0.1.0"
description = ""
requires-python = "^3.10"


[tool.poetry]
packages = [{ include = "mazegen" },{ include = "app" }]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
